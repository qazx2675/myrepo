# vCenter API 기반 VM IP 자동변경 — 계획서

## 1. 배경

네트워크 담당자가 특정 서버(BM 에 물린 VM)에 IP 를 잘못 할당해, 관리서버에서 그 VM 에
**네트워크로 접속할 수 없는** 상태다. 대상 VM 은 vCenter 에 BM/VM 형태로 등록되어 있고,
관리서버에는 PowerShell 과 Go 가 설치되어 있다.

기존에 이미 있는 `ip_change` 도구(같은 저장소의
[`Network_Change_Integration_Script/projects/ip_change`](../Network_Change_Integration_Script/projects/ip_change))
는 SSH(`gossh`)로 대상 노드에 접속해 `ifcfg-*` 를 고치는 방식이라, **네트워크가 이미
끊긴 지금 상황에는 쓸 수 없다.** 그래서 vCenter API(정확히는 VMware Tools Guest
Operations API)로 게스트 내부 명령을 실행하는 방식을 새로 만든다. 이 경로는
vCenter → ESXi → vmtoolsd 로 이어지는 관리 채널이라 VM 의 네트워크 스택을 거치지
않으므로, IP 오할당으로 접속이 끊긴 VM 에서도 동작한다.

## 2. 요구 흐름 (요청 원문 요약)

1. `vcenter.txt` — 작업 대상을 찾아야 하는 vCenter 명, 줄바꿈으로 구분해 입력
2. `list.txt` — `VM호스트네임 IP` 양식, 줄바꿈으로 구분해 입력
3. 만든 프로그램을 실행하면 `list.txt` 에 적힌 hostname 에 대해 기록된 IP 로 변경

## 3. 확정된 세부 규칙 (사용자 확인 완료)

| 항목 | 결정 |
|---|---|
| 서브넷마스크 | `255.255.255.0` 고정 |
| 게이트웨이 | 새 IP 의 마지막 옥텟을 `1` 로 바꾼 값 (예: `10.10.5.23` → `10.10.5.1`) |
| 대상 게스트 OS | 전부 RHEL 8.10 (NetworkManager/`nmcli`) |
| 인증정보 전달 | vCenter 로그인, 게스트 OS 로그인 모두 **환경변수**로 전달 |
| `VM호스트네임`의 의미 | vCenter 인벤토리상의 **VM 표시 이름** (저장소의 다른 프로젝트들과 동일 규약) |

## 4. 구현 방식

- 언어: Go (관리서버에 이미 설치됨, govmomi 로 vCenter API 를 직접 다룸)
- vCenter 접속: `vcenter.txt` 를 순서대로 순회하며 각 vCenter 인벤토리에서
  `list.txt` 의 호스트네임과 일치하는 VM 을 찾는다(먼저 찾은 vCenter 에서 처리하고,
  다음 vCenter 에서는 아직 못 찾은 대상만 계속 찾는다).
- IP 변경: 찾은 VM 이 **전원 켜짐 + VMware Tools 실행 중**이면, Guest Operations
  API(`StartProgramInGuest`)로 `/bin/bash -c '...'` 를 실행해 게스트 안에서
  - 현재 활성 NetworkManager 연결을 찾고
  - 그 연결의 `ipv4.addresses`/`ipv4.gateway` 를 `manual` 로 재설정한 뒤
  - `nmcli con up` 으로 그 연결만 다시 올린다 (서비스 재시작 없음).
  실행 결과는 `ListProcessesInGuest` 로 종료코드를 폴링해 확인한다.
- 인증정보: `VC_USER`/`VC_PASSWORD`(vCenter), `GUEST_USER`/`GUEST_PASSWORD`(게스트 OS)
  네 개의 환경변수로만 받는다. 파일에 평문 저장하지 않는다.
- 오프라인 빌드: 저장소의 다른 Go 프로젝트들과 동일하게 `vendor/` 를 포함해
  `GOPROXY=off` 로 폐쇄망에서도 빌드 가능하게 한다.

## 5. 산출물

경로: [`project/`](project/)

- `cmd/vm-ip-change/main.go` — 진입점(플래그 파싱, 대상 순회, 결과 출력)
- `internal/target` — `vcenter.txt`/`list.txt` 파싱, 게이트웨이 계산
- `internal/vsphere` — vCenter 접속, VM 조회, Guest Operations 로 IP 변경
- `vcenter.txt.example`, `list.txt.example` — 입력 파일 양식
- `setup.sh` — 폐쇄망 빌드 스크립트
- `README.md` — 빌드/실행 방법, 제약 사항

## 6. 제약 사항 / 다음에 필요할 수 있는 것

- RHEL 8.10 · NetworkManager 전제. 다른 배포판/도구(netplan 등)는 미지원.
- 활성 NIC 가 여러 개인 VM 은 첫 번째(= `lo` 제외 가장 먼저 나열되는) 연결만 변경한다.
  특정 NIC 를 지정하는 기능은 없다 — 필요해지면 `list.txt` 에 3번째 컬럼(연결 이름)을
  추가하는 식으로 확장한다.
- `list.txt` 의 새 IP 는 항상 `/24` 로 취급한다. 다른 서브넷마스크가 필요해지면
  `list.txt` 에 컬럼을 추가해야 한다(지금은 불필요해 만들지 않음).
- vCenter 인증서 검증을 하지 않는다(폐쇄망 자체서명 전제, 저장소의 다른 프로젝트와 동일).
