# 계획서 - 사용자별 작업 로그 자동 기록 시스템 (worklog-tracker)

## 1. 배경 / 문제

여러 명이 하나의 bash 스크립트를 공유해서 사용하는 환경(스크립트 내에서 `select`로
user를 고르는 방식)에서, 다음을 누락 없이 기록해야 함:

- 어떤 user가
- 어떤 인시던트 번호(REQ로 시작하는 고정 형식) 작업을,
- 언제 시작했고 언제 끝냈는지
- 그 작업 중 생성된 `vswitch_{user}.txt`, `spec_list_{user}` 산출물
- 작업 중 터미널 실행 로그(선택 기능)

## 2. 핵심 제약 조건과 그에 따른 설계 결정

| 제약 | 결정 |
|---|---|
| user는 스크립트 내 select로 자동 지정, incident는 사람이 입력 | `worklog_start(user)` 한 함수로 통합, incident는 내부에서 검증 루프 포함 입력받음 |
| 여러 명이 동시에 스크립트 실행 가능 | 락(lock) 대신: (1) user/incident 별로 파일 분리 (2) `all.log`는 한 줄 단위 `echo >>` append로 OS 레벨 원자성 확보 |
| 인시던트 번호는 REQ로 시작하는 고정 형식이나 정확한 자릿수 미확정 | `^REQ[0-9]+$` 정규식으로 우선 검증, 자릿수 확정되면 `lib/worklog.sh`의 `WORKLOG_INCIDENT_REGEX` 한 줄만 수정 |
| **스크립트 실행 종료 ≠ 작업 완료 시각** (완료 시점은 스크립트가 알 수 없음) | 자동 종료 감지를 포기하고, 완료는 **반드시 사람이 명시적으로 처리**하도록 설계 전환 (아래 3번 참고) |
| 기존 설정 스크립트에 영향 최소화 | 함수 라이브러리(`source`)로 제공, 기존 스크립트는 `worklog_start` 한 줄만 추가하면 됨 |

## 3. 설계 전환 과정 (왜 이 구조가 되었는가)

초기 아이디어는 `script(1)` 명령으로 세션을 감싸서 프로세스 종료를 "작업 완료"로
간주하는 방식이었으나, **실제 작업 완료 시점은 스크립트/터미널 세션 종료와 무관**하다는
점이 확인되어 폐기함. (예: 스크립트는 끝났지만 실제 작업은 나중에 수동으로 마무리되는 경우)

대안으로 다음 원칙을 채택:

- **START는 자동** — 스크립트 실행 시점에 무조건 기록됨 (사람이 잊을 수 없음)
- **완료는 사람이 명시적으로 처리해야만 기록됨** — 추정하지 않음
- 완료 안 된 건은 `logs/open/` 디렉토리에 파일로 남아있음 → **이 디렉토리 자체가 미완료 여부의
  단일 진실 소스(source of truth)**
- 미완료 건은 다음 스크립트 실행 시 자동으로 안내되어 **구조적으로 누락이 어려움**
- 단, 여러 건이 동시에 진행 중일 수 있으므로:
  - 목록으로 보여주고 **하나 선택**해서 처리 (전체 강제 완료 없음)
  - **스킵 가능** (0번 선택) — 새 작업 시작을 막지 않음
  - 독립 실행 가능한 `worklog-finish.sh`로 아무 때나, 여러 건 중 원하는 것만 골라 처리 가능

## 4. 최종 아키텍처

```
worklog-tracker/
├── lib/
│   └── worklog.sh          # 핵심 함수 라이브러리 (기존 스크립트에서 source)
├── bin/
│   ├── worklog-finish.sh   # 독립 실행: 미완료 건 중 하나 골라 완료 처리
│   └── worklog-status.sh   # 독립 실행: 미완료 건 조회
├── docs/
│   └── integration-example.sh   # 기존 스크립트에 붙이는 최소 예시
├── logs/                   # 실행 시 자동 생성 (최초 1회 git add 후 .gitignore로 내용 제외)
│   ├── open/               #   미완료 건 (파일 존재 = 미완료) - 상태의 단일 진실 소스
│   ├── users/               #   {user}.txt - user별 이력
│   ├── all.log              #   통합 이력
│   ├── artifacts/           #   완료 시 수집된 vswitch/spec_list 산출물
│   └── sessions/            #   (선택) 터미널 전체 로그 녹화본
├── 계획서.md
└── README.md
```

## 5. 로그 포맷

```
2026-08-14 15:32:07|kim|REQ1234567|START|
2026-08-14 18:05:00|kim|REQ1234567|END|end_time=2026-08-14 18:05:00;elapsed=2h33m
```

`|` 구분자 사용 (콤마 충돌 없음, 이후 집계/파싱 스크립트 작성 용이).

## 6. 기존 스크립트 통합 방법 (요약)

기존 스크립트에 딱 2곳만 수정:

```bash
# 최상단
source "/path/to/worklog-tracker/lib/worklog.sh"

# user 선택 직후
worklog_start "$SELECTED_USER"
```

이후 기존 로직은 전혀 수정하지 않음. 자세한 예시는 `docs/integration-example.sh` 참고.

## 7. 사용된 모델 / 리소스 참고

| 항목 | 내용 |
|---|---|
| 추천 모델 | Claude Sonnet 5 (bash 함수 라이브러리 수준 작업에 적합, 비용 대비 효율적) |
| 실제 작업 토큰 규모 | 설계 논의 + 코드 생성 포함 총 2~3만 토큰 수준 (다회차 질의응답 포함) |

## 8. 향후 확장 여지 (미구현, 필요 시 추가)

- `all.log` 기반 통계/대시보드 (user별 평균 소요시간, 미완료 건 알림 등)
- incident 번호 자릿수 확정 시 정규식 업데이트
- Slack/메일 알림 연동 (미완료 건 자동 리마인드)
