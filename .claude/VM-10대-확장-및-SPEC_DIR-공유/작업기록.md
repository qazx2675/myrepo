# 작업기록 — VM 1~10대 확장 + SPEC_DIR 공유 (V2)

계획서: `계획서.md` (결정 사항 D1~D12). 결과물: `.claude/VM/V2/`

## 2026-09-22 — 전 단계 완료 (Opus 5: 0~5단계, Sonnet 5: 6·8·9단계)

| 단계 | 내용 | 상태 |
|---|---|---|
| 0 | V2 뼈대: VM_setup → `V2/VMsetup`(vm-param-fix·바이너리 제외), vm-param-check-usability-improvement 복사, `V2/govendor`(A안) 구성, 복사본 10개 모듈 오프라인 빌드 확인 | 완료 |
| 1 | 회귀 기준선: 수정 전 V2 복사본 바이너리를 록키 `/root/v2work/baseline_bin`에 보관 | 완료 |
| 2 | vm-param-check ev01~ev10, `-specExport`, 스펙 `키=""` | 완료 |
| 3 | vm_create/affinity/lpage/tag ev01~ev10 (연속·값 없는 ev 제외) | 완료 |
| 4 | vswitch 병렬화 + 데이터센터 여러 개/폴더 여러 단계 대응 (vm_create/lpage/mac_info/main_conn) | 완료 |
| 5 | vm_create `-mapFile` VM 이름 키 + `nic_assign` 신설 | 완료 |
| 6 | `vm_setup.sh` 래퍼 (자동 할당 → y/n → 수동 선택 → vim, `a<번호>`) | 완료 (vcsim 21건 PASS) |
| 7 | vcsim 회귀/확장 검증 | 완료 (23/23 PASS) |
| 8 | home-test 실환경 검증 | 완료 (아래 참고) |
| 9 | 문서 보강(README/ARCHITECTURE/PR_CHECKLIST/CI), 태그 | 완료 |

### 검증 결과 요약 (록키 192.168.0.58)

- 빌드/정적분석: VMsetup 10개 모듈 `go build`/`go vet` 통과, vm-param-check `go test ./...` 통과, nic_assign 단위 테스트 통과
- vm-param-check 회귀: `-demo`, `-scale 300` 출력(콘솔/상세 CSV/요약 CSV) 수정 전후 바이트 동일
- VMsetup 회귀(vcsim, DC 1개·호스트 4대·ev01~ev03 12대): vm_create/affinity/lpage 결과 VM 덤프 차이 0건, 로그 동일
- 확장: 호스트 4대 × 10대 = 40대 생성, ev10 normal, affinity/lpage ev10
- 규칙: ev02 없이 ev03 → 에러, 값 없는 ev03 → 제외+경고, vmCount=1+ev02 없음 → 정상
- 데이터센터 3개 + 폴더 3단계: vm_create/lpage/affinity/vswitch/mac_info 정상 (수정 전 vm_create는 종료, lpage는 `please specify a datacenter`로 실패 — 문제 재현)
- 포트그룹 2개: 같은 BM에 2개 생성(수정 전 코드도 원래 지원), VM 키 매핑, nic_assign 변경·멱등
- 속도: DC 1개·호스트 20대 조회 경로 10회 평균 수정 전 0.0275s / 수정 후 0.0275s, DC 3개·호스트 60대 0.0534s

### 코드 확인으로 알게 된 것 (계획서 반영)

- 요청 5(ratio `normal`): 원래 지원됨 — 변경 없음, 경로만 테스트
- 요청 9(한 BM에 포트그룹 2개 이상): 원래 지원됨 — 병렬화만 추가
- vcsim은 클러스터의 폴더 이동을 지원하지 않아, 중첩 폴더 검증은 독립 호스트와 VM만 3단계로 옮겨서 했다

### 재현 방법

`검증/` 폴더의 시험 도구(vcsim 실행기 `vcsimenv`, VM 설정 덤프 `vmdump`, 시나리오 `scenarios.sh`, 속도 `timing.sh`).
빌드에는 `.claude/공통/govendor/govmomi-0.55.1-vcsim`을 `vendor`로 링크해야 한다.

```bash
cd 검증 && ln -sfn ../../공통/govendor/govmomi-0.55.1-vcsim vendor
go build -mod=vendor -o bin/vcsimenv ./cmd/vcsimenv && go build -mod=vendor -o bin/vmdump ./cmd/vmdump
# scenarios.sh 는 /root/v2work/{harness,baseline_bin,V2} 경로를 가정한다(록키 작업 디렉터리 기준)
```

---

## 2026-09-23 후속 점검·수정 (Opus 5.5 점검 → 사용자 결정 반영)

### 변경 요청 (양식)

- **대상**: `.claude/VM/V2/VMsetup` (`vm_create`, `vswitch_setting`, `affinity_setting`, `vm_setup.sh`, `vswitch_pgname.sh`), 예시 스펙, 문서
- **요청 내용 / 결정**
  1. FQDN 매칭이 vCenter 등록 방식에 의존하지 않게 → 파일·vCenter 어느 쪽이 FQDN/짧은 이름이어도 매칭(정확히 같은 이름 → 짧은 이름 비교, 짧은 이름이 여러 대면 오류)
  2. 실패는 종료코드 1로, **중복(이미 있는 포트그룹/VM)은 정상 종료**
  3. affinity 자동 계산 삭제, ev01~ev10 이 같은 파일을 써도 동작
  4·6. 이미 있는 VM 에 포트그룹 미적용 / 포트그룹 VLAN 다름 스킵 → 의도된 동작(변경 없음)
  5. IP 로 호스트를 등록하지 않음 → 변경 없음
  9. `vswitch_pgname.sh` 폴더명 Enter = 직전 값
  - 추가: `vm_setup.sh` vCenter 를 `vcenter.txt` 번호로 선택, user 별 이전 실행 vCenter 기억·출력
- **제약**: 기존 명령줄 호환(`-ht` 는 받아서 무시), 병렬 구조 유지, vm-param-check 체크 로직은 건드리지 않음
- **완료 기준**: vcsim 시험(`scenarios.sh`, `vmsetup_test.sh`) 전부 PASS + 새 동작별 시험 추가

### 결과

- `scenarios.sh` 32/32 PASS (회귀: 수정 전/후 바이너리에 같은 affinity 파일을 ev01~ev03 에 지정 → 덤프·출력 차이 0건), `vmsetup_test.sh` 34/34 PASS
- vcsim 은 이미 있는 포트그룹을 `DuplicateName` 으로 돌려줌(실제 ESXi 는 `AlreadyExists`) — 둘 다 "이미 존재"로 처리
- 실환경(home-test)에서는 이번 변경을 돌리지 않았다
- 상세: `.claude/VM/V2/VMsetup/CHANGELOG.md` 2026-09-23 항목

---

## 2026-09-23 호스트당 VM 99대로 확장

### 변경 요청 (양식)

- **대상**: `.claude/VM/V2` 전체(VMsetup 도구 4개 + `vm_setup.sh` + vm-param-check)
- **요청 내용**: VM 10대 → 최대 100대. 방식은 그대로, 수량만 늘린다.
- **결정**: 상한은 **99**(이름 `ev%02d` 두 자리 유지 — 100이면 `ev100`이 `ev10`으로 판정되는 문제). `-h` 도움말은 ev02~99를 한 줄로 묶는다.
- **완료 기준**: vcsim 시험 전부 PASS + 99대 생성/적용/100 거부/도움말 시험 추가

### 결과

- `scenarios.sh` 39/39 PASS (S6 추가: 2호스트 × 99대), `vmsetup_test.sh` 36/36 PASS (V11 추가: ev12 스펙), `go test` 통과
- `vm_setup.sh` vim 템플릿은 ev10까지만 빈 틀(ev11~ev99는 직접 줄 추가)
- 실환경(home-test): 192ev03~192ev99 97대 생성 22초, affinity 99대 7초, lpage 97대 5초 — 모두 성공, 기존 192ev01/02 변경 없음, vm-param-check ev57/ev99 그룹 판정 확인. 정리 도구 `labtool rmvm` 허용 범위를 192ev03~192ev99로 넓힘
- 상세: `.claude/VM/V2/VMsetup/CHANGELOG.md` 2026-09-23 "호스트당 VM 1~99대" 항목
