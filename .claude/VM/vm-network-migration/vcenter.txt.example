# vCenter 주소 목록. 한 줄에 하나씩 적습니다. 여러 대를 적으면 모두 동시에 조회합니다.
# 빈 줄과 # 로 시작하는 줄은 무시됩니다.
#
# 계정 정보는 이 파일에 넣지 않습니다. 환경 변수로 전달하세요.
#   export VC_USER='administrator@vsphere.local'
#   read -rs -p 'vCenter 비밀번호: ' VC_PASS; export VC_PASS; echo
#
# 이 파일을 vcenter.txt 로 복사해서 쓰세요.
#   cp vcenter.txt.example vcenter.txt

vcenter-a.example.local
vcenter-b.example.local
10.10.20.50
