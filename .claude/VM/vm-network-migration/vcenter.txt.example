# vcenter.txt — 대상 vCenter 주소 목록 (한 줄에 하나)
# 계정 ID 는 -id 플래그(기본 lscsystems@vsphere.local),
# 비밀번호는 환경변수 VC_PASSWORD 로 넘깁니다. 이 파일에는 주소만 적습니다.
# 빈 줄과 # 로 시작하는 줄은 무시합니다.
192.168.0.50
# vcenter-b.example.local
