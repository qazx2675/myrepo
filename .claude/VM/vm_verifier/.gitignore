# 컴파일된 바이너리
vm-verifier

# 실행 시 생성되는 UUID 이력 파일 — 환경마다 다르고 실제 인프라 정보를 담으므로 저장소에 안 올림
vm-verifier-uuid-history.json
*.log

# 불일치 감지 시 생성되는 감사 로그 폴더 — 실제 인프라 정보를 담으므로 저장소에 안 올림
LOG/

# 배치 모드 입력 파일 (vCenter 주소, 대상 BM 목록) — 실제 인프라 정보가 들어가므로
# 저장소에 올리지 않는다. README의 사용법 예시를 보고 각자 환경에 맞게 만들 것.
vcenter.txt
targets.txt
