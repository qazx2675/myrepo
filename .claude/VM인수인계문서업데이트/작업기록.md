# VM 인수인계 문서 업데이트 — 작업 기록

- 작업일: 2026-09-24
- 요청: 같은 폴더 `계획서.md`(사용자 제공 업데이트 계획서) 대로 `.claude/VM/인수인계/` 문서 세트를 최신 저장소 기준으로 재작성
- 산출물: `.claude/VM/인수인계/*.md`, `build_handbook.py`, `vendor/mermaid.min.js`, `VM_인수인계_핸드북.html`

---

## 1. 작업 전 확인한 사실 (계획서와 다른 점)

| 항목 | 확인 결과 | 처리 |
|---|---|---|
| 원본 `.md` 위치 | 저장소에 커밋된 적 없음. `Downloads/VM스크립트 인수인계/VM_인수인계_문서.tar.gz`에만 존재 | tar.gz 에서 복원해 **먼저 원본 그대로 커밋**(786f406), 그 위에서 수정 |
| V2 ↔ VM_setup | `VM_setup`의 9개 `*-source`가 전부 `V2/VMsetup`에 있음(+ nic_assign, vm_setup.sh, vswitch_pgname.sh 신규). `vm-param-fix`는 V2 README에 "대체됨" 명시 | `VM_setup`에서 남는 것은 **`power_setting` 바이너리 하나** → 17번 문서 |
| vm-param-check | V1 폴더와 `V2/vm-param-check-usability-improvement` 두 벌. V2가 최신(ev01~ev99, `-specExport`, 스펙별 동질성 게이트) | 사용자 결정: **V2 기준** |
| vm-network-migration | `Network_Change_Integration_Script/projects/vm-network-migration/`에 포함, E 단계에서 `run.sh` 호출 | 통합됨으로 명시 |
| `vCenter API IP 자동변경` | vCenter 의 IP 가 아니라 **접속 불가 VM 의 IP 를 vCenter API 로 재설정**하는 도구(vm-ip-change) | 전체 흐름도에서 망변경 뒤 "접속 불가 VM 복구" 분기로 배치 |
| 표에 없는 새 폴더 | 없음 (`test_all.sh`는 파일) | — |

## 2. 사용자와 확정한 사항

| 항목 | 결정 |
|---|---|
| 11번 기준 소스 | V2 (`V2/vm-param-check-usability-improvement/vm-param-check`) |
| 원본 처리 | 복원 후 커밋 → 수정 커밋 |
| 중복 처리 원칙 | **V2 우선, V2가 아니면서 중복되는 것은 모두 제외** |
| 참고 서버 | 192.168.0.58 (비밀번호 없이 SSH) |

중복 제외 원칙에 따라 `integrated-vm-param-check-test-tool`도 제외했다(vm-param-check 사본 + 시험용 vc-test-env 뿐이라 V2와 중복/테스트). 계획서의 16번 문서는 만들지 않고 40번 폴더구조에 한 줄 안내만 남겼다.

## 3. 문서 변경

| 구분 | 파일 |
|---|---|
| 삭제 | 00_시작하기, 10_VM_setup, 14_vm-network-migration, 15_esxi-log-check, 16_vm-param-setting-check, 17_vm-setting-go-lang, 18_vcenter-test-env-vcsim, 19_integrated-vm-param-check-test-tool, 20_gemini_vcsim-pipeline-test, 21_powershell, 계획서.md(구 작업기록 폴더에 동일본 있음) |
| 신규 | 00_빠른시작, 10_V2, 14_Network_Change_Integration_Script, 15_vCenter_API_IP_자동변경, 17_VM_setup_잔여도구 |
| 이름 변경+재작성 | 11_vm-param-check-usability-improvement → 11_vm-param-check (V2 기준) |
| 재작성 | README(문서 지도·전체 흐름도·불일치 목록), 02, 12, 13, 31, 40, 91, 99 |
| 부분 수정 | 01(표현, ev01~ev99, vcsim 절 삭제, 링크), 30(안전장치 표·예시 양식 경로), 90(테스트·제외 도구 용어 삭제, 망변경 용어 추가) |
| 빌드 | `build_handbook.py`: ORDER 갱신, ```mermaid → `<div class="mermaid">`, `vendor/mermaid.min.js`(mermaid 10.9.1, 3.3MB)를 HTML 에 인라인, 테마 전환 시 재렌더 |
| 저장소 README | `.claude/VM/README.md`에 Network_Change_Integration_Script, vCenter API IP 자동변경, 인수인계 항목 추가, vm-network-migration·vm-param-setting-check 에 "통합됨" 표기 |

모든 도구 문서는 `한눈에 보기 → 바로 쓰는 명령어 → 흐름도 → 준비물 → 옵션표 → 결과 확인 → 오류 → 주의사항 → 파일 구조` 순서.

## 4. 소스 기준으로 바로잡은 것

- **affinity 파일 형식**: 기존 문서의 `0=0,1`은 틀림. 실제는 `sched.vcpuN.affinity=0,1` (키가 그대로 ExtraConfig 로 들어감, `affinity_setting-source/main.go loadAffinitySpec`, `SPEC_DIR/.../affinity_ev01.txt`).
- **동질성 게이트**: V2 는 "같은 스펙·같은 그룹"끼리만 비교, 그룹 간 VM 대수 차이는 경고만(`fixer/gates.go`). 기존 문서는 대수 불일치도 게이트라고 적혀 있었음.
- **power_setting 옵션**: 문서에 없던 옵션을 .58 에서 바이너리 `-h`로 확인 — `-vcTargetIP`, `-id`(기본 `lscsystems@vsphere.local`), `-worklistFile`(기본 `worklist_bm.txt`). 비밀번호 환경변수 `VC_PASSWORD`는 바이너리 문자열로 확인.
- **vm_verifier 빌드**: `vendor/`가 아니라 `../../공통/govendor/govmomi-0.39.0` 링크 → 반출 시 `.claude/공통/govendor` 동반 필요.

## 5. 소스–README 불일치 (README.md 에 기록)

| 도구 | 내용 |
|---|---|
| V2 `VMsetup/*-source` 6종 README | 빌드 예시 경로가 V1(`.claude/VM/VM_setup/...`) |
| V2 `VMsetup/README.md` 공통 규칙 | `-vcTargetIP`/`-worklistFile`이 공통이라 적었지만 numa_preferht(`-vc`/`-f`), tag_setting(`-hostListFile`), nic_assign(`-mapFile`)은 다름 |
| `integration.conf.sample` 주석 | `-tag` → 실제는 `--tag` |
| `vm_verifier/setup.sh` 주석 | `go mod vendor` 안내 → 실제는 공통 govendor 링크 |

기존 목록의 esxi-log-check / vm_lpage_bulk / vm_create(vm-setting-go-lang) 3건은 제외 폴더라 삭제.

## 6. 확인

- `grep -rniE "demo|scale|vcsim|go test|testkit|검증 이력"` → 2건, 모두 40번의 제외 폴더 **이름**(`gemini_vcsim-pipeline-test`, `vcenter-test-env-vcsim`) — 계획서 예외
- `grep -rnE "초보|처음 보|처음 접|처음 오"` → 0건
- 문서 간 상대 링크 깨짐 0건
- HTML 태그 짝 검사(html.parser) → 미닫힘 0, 불일치 0
- 브라우저(로컬 HTTP, 외부 요청 없음)에서 mermaid 8개 전부 SVG 렌더, 다크 테마 전환 후 재렌더 확인. 페이지 내 외부 `script src`/`link href` 0개
- `python build_handbook.py` → 17개 문서, 약 3.5MB

## 7. 남은 것 / 참고

- 원본 README 불일치 4건은 문서에만 기록했고 원본 README·주석은 고치지 않았다(범위 밖).
- power_setting 의 호스트 이름 매칭 규칙 등 세부 동작은 소스가 없어 확인하지 못했다(17번 문서 7절에 명시).

---

# 2차 업데이트 (2026-09-25)

- 요청: V2 `VMsetup`, `vm-param-check-usability-improvement` 변동사항 반영 / 전체 흐름도에서 VM setup 단계와 운영 단계를 분리하고 운영을 "망/인프라 변경"으로 / 흐름도 디자인 개선. 저장소 `workflow.svg` 디자인 변경은 사용자 결정으로 제외.
- 반영 범위: `13a5b16..HEAD`의 `.claude/VM/V2` 커밋 12건 (b6628a0 ~ a74c638)

## 1. 반영한 V2 변동사항

| 변경 | 반영 문서 |
|---|---|
| V2 폴더 통째 배치(`/home/V2`), 상대 위치로 서로 찾음 | 00, 02, 10, 11, 17, 31, 99 경로 전부 `/home/V2/...` |
| `passwd_update.sh`/`secret_lib.sh` 암호 파일, 실행 시 셸 비밀번호 변수 `unset` | 10(비밀번호 절·1절), 11, 00, 02(환경변수 표), 91, 99 |
| `vm_setup.sh` `-id`(기본 `lscsystems@vsphere.local`, `-i` 동일), user 번호 메뉴, CAE 번호 무시 매칭·변경, 색상, BM→스펙 확인 표 삭제, affinity 추가 질문, 생성 후 스펙 체크(`check_<번호>.*`), 계획 ev 범위 묶음 | 10 (1·3·4·5·6·7·9절) |
| `vswitch_<user>.txt` 위치 `SPEC_DIR/` → `VMsetup/` | 02, 10, 14 |
| SPEC_DIR 탐색 순서 `-s` → `vm-param-check/SPEC_DIR` → `V2/SPEC_DIR` | 02, 10 |
| OS6: `setup.sh --os6`, `VMSETUP_OS6`, `OS6_hostname`, `build_os6.sh`, `bin_os6/` | 10(4절 표·오류·파일구조), 11, 31 |
| vm-param-check `-specFolder` | 11 (1·2·4절) |
| `vm_setting_check_insert.sh` `-u`/번호 메뉴, `-id`, 암호 파일 | 11 (8절) |
| 콘솔 요약 표 헤더 정렬 수정 | 문서 영향 없음 (표시 버그 수정) |

## 2. 흐름도

- README 전체 흐름도: `subgraph` 두 개 — **① VM setup 단계**(호스트 등록 → VM 인도) / **② 망/인프라 변경 단계**(망변경, vm-ip-change, 재점검). 기존 "운영" 노드 삭제.
- 디자인: `build_handbook.py`에서 mermaid `base` 테마 + 밝게/어둡게 별도 색, 곡선 연결선, 둥근 모서리·그림자, 단계 박스 점선. 공통 색 클래스(`input`/`cmd`/`change`/`gate`/`warn`/`safe`)를 렌더 시 자동으로 넣고, 각 문서 흐름도는 `class ... 이름` 줄로 지정만 함(GitHub 에서는 색 없이 그대로 보임).
- 10번 흐름도를 새 `vm_setup.sh` 순서(user 선택 → 스펙 → 포트그룹 → CAE 번호 → vCenter → 계획 → 실행 → 스펙 체크)로 다시 그림. 11번에 `-specFolder` 분기 추가.

## 3. 소스–README 불일치 추가

- V2 `README.md` 비밀번호 절은 "환경변수 → 암호 파일 → 입력"이라 적었지만, 두 스크립트 모두 시작 시 비밀번호 변수를 `unset`함 → README.md 불일치 목록에 추가.

## 4. 확인

- 금지 표현 0건, 시험 용어 2건(제외 폴더 이름, 기존과 동일), 상대 링크 깨짐 0건
- 로컬 HTTP 로 HTML 확인: mermaid 8개 모두 SVG, 어둡게 전환 후 재렌더 확인

---

# 3차 업데이트 (2026-09-25) — 흐름도 범례

- 요청: 흐름도 SVG 도형별 의미를 11시 방향에 작게 표시 (사용자 제공 도형표 참고). 저장소 `workflow.svg`는 이전 결정대로 계속 제외.
- `build_handbook.py`: `MERMAID_INIT`에 `addLegend()` 추가. 각 흐름도의 mermaid 소스(`data-src`)를 검사해 **그 흐름도에 실제로 쓰인 기호만** 골라 범례로 표시.
  - `◇ 예/아니오` — `{"..."}` 마름모가 있을 때만
  - `▭ 처리` — 항상 (모든 흐름도가 사각 노드를 씀)
  - `⇢ 참고/생략 가능` — 점선 화살표(`-.->`)가 있을 때만 (14·15번)
  - `⇒ 단계 전환` — 굵은 화살표(`==>`)가 있을 때만 (README), 없으면 `→ 다음 단계`
  - `⬚ 단계 묶음` — `subgraph`가 있을 때만 (README)
  - 시작/종료를 나타내는 타원형·연결점 원형은 현재 어느 흐름도에도 쓰이지 않아 범례에도 넣지 않음(사용자 확인 없이 도형을 새로 추가하지 않음)
- CSS: `.mermaid`에 `position:relative`, `.hb-legend`는 절대위치 좌상단(11시 방향), 10px 작은 글씨, `var(--muted)` 색, 인쇄 시 숨김.
- 확인: 로컬 HTTP로 8개 흐름도 전부 범례 표시 확인(README는 처리/단계 전환/단계 묶음만, 11번은 예아니오/처리/다음 단계만 등 흐름도별로 다르게 나옴), 밝게/어둡게 전환 후에도 잘 보임.

---

# 4차 업데이트 (2026-09-25) — 범례 가독성 개선

- 요청: 범례를 1.5배 크게, 세로 나열, 더 진하게. 특히 README 첫 흐름도(맨 위)에서 잘 안 보임.
- 원인: 유니코드 도형 문자(▭ 처리, ⬚ 단계 묶음)가 일부 글꼴에서 빈 네모(tofu)로 깨져 표시됨. `◇`/`→`/`⇒`는 정상 표시.
- 수정 (`build_handbook.py`):
  - 유니코드 문자 대신, 실제 mermaid 노드 모양과 같은 작은 아이콘을 **CSS로 직접 그림** — 처리(둥근 네모 테두리), 마름모(45도 회전 정사각형 테두리), 단계 묶음(점선 네모), 화살표/굵은 화살표/점선 화살표(막대 + 화살촉).
  - `.hb-legend`: 세로 배열(`flex-direction:column`), 글자 15px(기존 10px, 1.5배), 굵게(700), 배경을 사이드바색+굵은 테두리+그림자로 확실히 구분, 항목 간격 6px, 아이콘도 21×15px로 커짐.
- 확인: 로컬 HTTP로 README(맨 위)·10번(마름모 포함) 흐름도에서 범례가 크고 또렷하게 보임, 밝게/어둡게 전환 모두 확인.

---

# 5차 업데이트 (2026-09-25) — 전체 흐름도 단계 보완, y/n 라벨 가독성

- 요청: VM setup 단계에 빠진 단계 보완 — VM_create(vm_setup.sh) 다음 MAC 수집 다음 power_setting, lpage 다음 license_assign. 화살표 위 y/n 라벨이 흐릿하게 보이는 문제 수정.
- README 전체 흐름도(① VM setup 단계) 순서 변경:
  - `license_assign`을 A(호스트 등록)에서 분리해 B(lpage_search) 다음 새 노드로 이동
  - `power_setting`을 H1(사후 -fix remedial)에서만 언급하던 것을 E(mac_info) 다음 새 노드로 추가 — 파워온 전에 호스트 전원정책을 미리 맞춰 두는 표준 단계로 승격, H1은 "남아 있으면 재실행"으로 문구 수정
- [17_VM_setup_잔여도구.md](../VM/인수인계/17_VM_setup_잔여도구.md)에 "언제 실행하나요?" 문단 추가 — mac_info 다음·vm_verifier 전이 표준 위치임을 명시 (멱등이라 재실행해도 안전)
- y/n 라벨 가독성: `build_handbook.py`의 `.mermaid .edgeLabel` — 배경 span에 `padding`, `border-radius`, `font-weight:700`, `box-shadow`를 `!important`로 강제(짧은 한 글자 라벨이 배경 없이 교차선 위에 떠서 흐려 보이던 문제), 글자 12.5px → 13.5px.
- 확인: 로컬 HTTP로 README·10번 흐름도에서 새 순서·굵어진 y/n/예/아니오 라벨 확인, 밝게/어둡게 모두 확인.

---

# 6차 업데이트 (2026-09-26) — 10번 문서 내부 흐름도에도 반영

- 확인: 5차 업데이트(README 전체 흐름도)는 이미 diff상 순수 추가였고 기존 내용을 지우지 않았음. 사용자가 실제로 말한 대상은 **10_V2.md의 vm_setup.sh 내부 흐름도**였음(AskUserQuestion으로 확인).
- 10_V2.md 흐름도는 `vm_setup.sh`가 실제로 호출하는 순서(vswitch→vm_create→affinity→lpage)를 그린 것이라, mac_info·power_setting·license_assign은 스크립트가 부르지 않는 별도 도구임을 먼저 본문에 한 줄로 밝힘(정확성 유지).
- 기존 파이프라인(H~O, 실패 판정 L 포함)은 전혀 손대지 않고, 두 지점에만 순수 추가:
  - `L -- 아니오 -->` 바로 다음에 `license_assign`(신규 호스트만) 삽입 → 스펙 체크(N)로 연결
  - `[완료](M)` 다음에 `mac_info` → `power_setting` 삽입 → 기존 마지막 안내(O)로 연결
- `git diff`로 삭제된 줄이 없음을 확인(추가만 3곳).
- 로컬 HTTP로 8개 흐름도 전부 렌더 확인, 새로 추가된 두 구간 스크린샷으로 순서 확인.

---

# 7차 업데이트 (2026-09-26) — mac_info·power_setting 위치를 vm_create 바로 다음으로 재조정

- 사용자가 브라우저 화면에 직접 화살표를 그려서 지적: mac_info·power_setting을 파이프라인 맨 끝([완료] 다음)이 아니라 **vm_create 바로 다음, affinity_setting 이전**에 두라는 것이었음.
- 10_V2.md 수정: `I(vm_create) --> P(mac_info) --> P1(power_setting) --> J(affinity_setting)`로 재배치. license_assign 위치(lpage 이후 성공 분기)는 그대로.
- 설명 문구도 실제 배치에 맞게 수정.
- 겸사겸사 같은 요청을 별도 세션이 만든 `.claude/VM/V2/VMsetup/workflow.svg`(vm_setup.sh 자체 흐름도, 인수인계 폴더 밖)에도 반영 — 소스 `.mmd`에 순수 추가 후 오프라인 mermaid.min.js로 재렌더링. 자세한 기록은 `.claude/V2 흐름도 SVG 추가/작업기록.md` 참고.

---

# 8차 업데이트 (2026-09-26) — V2/VMsetup 모니터·통합 반영 + 목차 재구성

## 1. V2/VMsetup 모니터 업데이트 반영

별도 세션이 `.claude/VM/V2/VMsetup`에 큰 변경을 넣었다(커밋 f161a1a, 2c1ffa1, aaa3b27, 9f6f10a 등, `git merge origin/master`로 반영):

- **`vm_setup.sh` 진행 모니터(신규)**: 실행 확인(y) 이후 도구 출력을 흘리지 않고 단계별 요약 화면(↑↓ 이동·Enter 로그·Ctrl+C 3번 종료)으로 보여줌. `VMSETUP_MONITOR=0`으로 끔. 로그는 `run_<user>/logs/`, `run.log`, `plan.txt`.
- **`mac_info`·`power_setting`·`license_assign`이 이제 진짜로 `vm_setup.sh` 내부 호출**로 편입됨(전에는 "별도 후속 도구"였던 게 사실이 되었음). 스펙마다 `vm_create → mac_info → power_setting → affinity_setting → lpage_setting`, 모든 스펙이 끝나면 `license_assign`을 BM 전체 대상 1회.
- **`power_setting-source`(신규, 소스 있음)**: V2 자체 전원정책 도구. 이제 [17번 문서](../VM/인수인계/17_VM_setup_잔여도구.md)의 V1 바이너리(소스 없음)를 대체함 — 10_V2.md의 "V1 대체 범위" 표에서 "대체 없음"으로 적었던 것을 정정.
- **설치 정보 단계(신규)**: user 선택 다음 OS 버전/인프라를 매번 물어봄(건너뛰는 환경변수 없음), `mac_info`에 자동 전달.
- **MAC 디스크 자동매칭**: `sda5` 다음 값을 스펙 `disk` 값 기준으로 고정 용량 중 가장 가까운 값으로 자동 변환.
- **`awx_route`**: 스펙마다 모은 MAC 목록(`run_<user>/mac_all.txt`)을 전 스펙이 끝난 뒤 그 폴더에 복사.
- **빌드 대상 11종 → 12종**(`power_setting` 추가) — 관련 문서의 "11종"을 모두 "12종"(VMsetup은 "10종"→"11종")으로 수정.

반영한 문서:
- **10_V2.md**: 한눈에 보기·V1 대체 범위 표·흐름도(전면 재작성 — 통합 파이프라인 + 설치정보 + MAC 합본 복사 노드)·진행 모니터 절(신규)·준비물(설치정보/awx_route 절 신규)·5-1 옵션표(VMSETUP_MONITOR)·5-9(power_setting 행 추가 + 자동 호출 설명)·6절(모니터 로그 행)·7절(모니터·설치정보 오류 행)·9절(파일 구조에 logs/, run.log, plan.txt).
- **17_VM_setup_잔여도구.md**: "남아 있는 이유"를 "V2 대체 없음"에서 "V2 에 소스 있는 대체 도구가 이미 있음"으로 정정. 새로 작업할 땐 V1 바이너리 대신 V2 쪽을 쓰라고 명시.
- **README.md 전체 흐름도**: `license_assign`(B1)·`mac_info`+`power_setting`(E, E1) 별도 노드를 삭제하고 D(VM 생성·설정) 노드 하나로 통합(vm_setup.sh 가 이제 다 부르므로). vm_verifier 노드에 MAC 목록 출처(mac_all.txt/awx_route) 추가.
- **00/02/40/99**: "11종"→"12종", "10종"→"11종" 수정.

## 2. 목차(문서 지도) 재구성

요청: 아래 4개를 메인으로 묶고 나머지는 아래로.
- VM setup 및 설정 체크: 10_V2(VMsetup) + 11_vm-param-check
- 망변경: 14_Network_Change_Integration_Script + 15_vCenter_API_IP_자동변경
- 나머지(12_vm_verifier, 13_lpage_search, 17_VM_setup_잔여도구)는 그 아래로.

파일 이름(문서 번호)은 그대로 두고(링크가 저장소 곳곳에 걸려 있어 이름 변경은 위험이 큼), **표시 순서와 그룹 헤더만** 바꿈:
- `build_handbook.py` `ORDER`: 10,11,14,15,12,13,17 순으로 재배열(핸드북 사이드바·읽는 순서에 반영).
- `build_handbook.py` `groups`: "VM setup 및 설정 체크"(10 부터), "망변경"(14 부터), "그 외 도구"(12 부터) 헤더 추가.
- `README.md` "도구별 문서": 표 하나를 3개 표(### VM setup 및 설정 체크 / ### 망변경 / ### 그 외 도구)로 나눔. 17번 설명에 V2 대체 도구 안내 추가.

## 3. 확인

- `git diff`로 각 파일이 순수 수정(삭제 없이 정정·추가)인지 확인.
- 소스 근거: `VMsetup/CHANGELOG.md`, `VMsetup/README.md`, `vm_setup.sh` 본문(`hdr "설치 정보"`, `ensure_bin` 순서 등), `power_setting-source/README.md`, `V2/setup.sh`의 `TARGETS` 배열(12개 확인).
- 로컬 HTTP로 핸드북 재생성 확인: mermaid 8개 렌더, 사이드바 그룹 헤더 순서(`시작 → VM setup 및 설정 체크 → 망변경 → 그 외 도구 → 유지보수 → 찾아보기`) 확인, 10번 문서 새 흐름도(설치정보→...→license_assign 통합)와 README 단순화된 메인 흐름도 스크린샷으로 확인, 밝게/어둡게 모두 확인.
- 상대 링크 깨짐 0건, 금지 표현 0건.
