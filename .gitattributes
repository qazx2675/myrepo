# 이 스크립트는 Linux(폐쇄망 서버)에서 실행됩니다.
# Windows 에서 clone 하더라도 반드시 LF 로 체크아웃되어야 합니다.
# CRLF 로 나오면 bash 가 "set: pipefail: invalid option name" 등으로 죽습니다.
* text=auto eol=lf
*.sh   text eol=lf
*.go   text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
*.conf text eol=lf
*.sample text eol=lf
